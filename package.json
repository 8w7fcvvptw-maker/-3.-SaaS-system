{
  "name": "saas-system",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server/auth-api.mjs",
    "dev": "concurrently -n api,web -c blue,green \"node server/auth-api.mjs\" \"npm run dev --prefix frontend\"",
    "dev:server": "node server/auth-api.mjs",
    "dev:frontend": "npm run dev --prefix frontend",
    "notifications:scheduler:run": "node server/notification-scheduler.mjs",
    "supabase:start": "npx supabase@latest start",
    "supabase:stop": "npx supabase@latest stop",
    "functions:serve": "npx supabase@latest functions serve notify-appointment-telegram --env-file supabase/.env.functions",
    "plugins:vercel": "npx plugins add vercel/vercel-plugin -t cursor --scope project -y"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.2",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
